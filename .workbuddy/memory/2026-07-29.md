# 2026-07-29 工作日志

## 2DRPG 实时地图决策效果测试 demo（experiment-sandbox/2drpg-realtime-map）

在 experiment-sandbox 下新建 `2drpg-realtime-map` 沙箱，验证《2DRPG 实时地图与实时互动系统设计》（总规划+模块1-7+附录A-E）的整体效果与决策分叉口视觉/交互差异。

### 技术选型（对齐主项目）
- 依样复制 packages/frontend 主链路：React 18 + TS 5.3 + Vite 5.4 + Tailwind 3.4 + Zustand 4，暗色主题 variables.css，端口 5200
- 代码风格：2 空格/单引号/分号必加/PascalCase 组件，与 .prettierrc 一致
- 新增依赖：three@0.169（附录D 候选项E 真动态光影）

### 实现的决策分叉口（控制面板 7 分区）
1. 渲染器决策（附录D）：Canvas2D 等距 vs Three.js 2.5D，正交/透视/混合投影
2. 模拟光源（要求#3）：昼夜循环 + 玩家火把径向光(destination-out+lighter) + 建筑落影
3. 瓦片资源映射（附录A）：MVP色块+emoji / 程序化俯视纹理32×32 / 等距菱形精灵图64×32 三模式热切换
4. 地图生成器（模块2）：程序化值噪声fbm vs LLM模拟（结构化群落+道路+地标）
5. 建筑内部方案（模块6/附录C）：方案A 独立地图 vs 方案B Z层（F键楼梯切层，small_house 模板）
6. 预判触发（模块4）：P0-P3 优先级开关 + 触发距离 + 预生成进度可视化
7. 实时监控（模块7）：FPS/DrawCall/精灵数/纹理内存/缓存命中率/玩家区块

### 关键文件
- src/types/tile-map.ts：附录A 15种TileType+TILE_PROPERTIES+坐标变换+附录D ITileMapRenderer端口
- src/core/map-data.ts：mulberry32 PRNG + 值噪声 + 双生成器 + ChunkStore(LRU)
- src/core/tileset.ts：程序化生成所有瓦片纹理到离屏canvas（验证资源映射替换）
- src/renderers/Canvas2DIsoRenderer.ts：等距投影+视口裁剪+2.5D建筑盒+伪光源
- src/renderers/ThreeIsoRenderer.ts：WebGL真2.5D+DirectionalLight/PointLight+ShadowMap+FogExp2
- src/store/demoStore.ts：Zustand 持有全部决策状态+地图运行时+性能指标
- src/components/{MapCanvas,ControlPanel,panels,ui/controls}.tsx

### 环境坑（重要）
- **npm install 在沙箱内会静默失败(exit 1 无输出)**，必须用 `dangerouslyDisableSandbox: true` 在前台跑才成功（npmmirror 镜像 30s 装完）。后台任务即使带该参数也不可靠。
- pnpm corepack 路径损坏（`C:\c\Program Files\...` 被错误拼接），改用 npm。
- vite 启动时 5200/5201 被占用，实际落到 5202。

### 运行
`cd experiment-sandbox/2drpg-realtime-map && npm run dev` → http://127.0.0.1:5202
typecheck 已通过（tsc --noEmit exit 0），所有模块 vite 转换 200。

## Ardot 设计画布连接阻塞（设计任务未推进）

用户要求「优化前端界面 + 绘制游戏图标」，引用 scene#37「图标&插画」，已加载 ardot-design-core + ardot-poster 技能。

- `create_design` 返回"document is loading"，但后续 `fetch_editor_state` / `fetch_file_info` 在多轮重试（含 sleep 5s/6s/10s 等待）后**持续返回 `NO_ADAPTER: routeKey 42836420-8ab5-44f0-8ace-144446a53d26`**。
- 用户称已在桌面端"打开画布"，但 agent 侧 ardot 适配器始终未连接——这是 agent 会话与画布后端之间的连接问题，与本地 UI 是否打开文件无关。
- 结论：本会话设计画布不可用，按 ardot 硬规则停止，未用任何替代路径伪造设计输出。
- 待办：需用户在 agent 侧真正连上画布适配器（重连/重载设计画布会话）后，我才能读取 scene#37 并绘制图标（攻击/防御/生命/金币/宝石/魔法/速度/钥匙/宝箱/星级等统一网格集）与界面 HUD。

## 2DRPG 实时地图决策验证沙箱（2drpg-realtime-map）
- 在 `experiment-sandbox/2drpg-realtime-map` 建成《2DRPG 实时地图与实时互动系统设计》全决策点验证 demo（总规划+模块1-7+附录A-E 共 13 份文档已全部通读）。
- 技术栈对齐主项目：Vite5+React18+TS strict+Tailwind+Zustand；端口 5200（dynamic-ui-sandbox 用 5199）。
- 架构：React 外单例 WorldEngine（组合根注入）+ core/ 纯函数层（scheduler/result-pool/lru-cache/fov/pathfinder/noise/buildings）+ render/（Canvas2DRenderer 等距2.5D + CssGridRenderer）+ 控制面板 8 分区。
- 决策分叉口全部可 A/B：渲染器/风格/边界策略AB/边界美化/内部方案AB/区块大小/生成器路由/ATL-JSON/规模/mock。
- 暗黑 2.5D：等距投影+墙体棱柱+火把闪烁+熔岩自发光+昼夜+暗角；ImageGen 生成暗黑风 4×4 图集（public/sprites/iso-tiles-dark-fantasy.png）验证资源映射换肤（chroma-key 抠黑底）。
- 验证：tsc --noEmit 零错误；playwright 冒烟零控制台错误；截图证据在 scripts/*.png（含方案A/B 内部渲染对比）。
- 经验：rAF 游戏循环内渲染配置必须用 useConfigStore.getState() 每帧读取，React 闭包捕获会导致配置不生效；出生点需 snapPlayerToWalkable 防止落在水域。
